'use strict';

var eventsApp = angular.module('eventsApp', []);
//creating a module called events app
//modules are the basic building blocks of Angular
//Everything exsists inside of a module
//The empty array tells angular this module does not depend on any other module
//Capturing that module into a global variable called eventsApp this makes it easy to create objects inside of that module such as controllers and services